// Canonical, publicly-served origin for the site.
// This MUST match the domain Vercel serves 200s on (the apex 308-redirects here).
// Every SEO signal (canonical tags, sitemap, robots, JSON-LD) must use this exact host.
export const SITE_URL = 'https://www.mypawcraft.com'
